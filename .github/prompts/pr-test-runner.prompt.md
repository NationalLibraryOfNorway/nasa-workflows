---
description: 'Test a PR branch locally using git worktree — run tests and report results'
mode: 'agent'
agent: 'pr-test-runner'
---

Test a PR branch locally using git worktree. Create an isolated checkout, run the relevant tests, and report results.

Task: $PROMPT_INPUT

